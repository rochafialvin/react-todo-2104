Satu buah komponen hanya boleh return 1 buah element

State adalah sebuah variabel spesial ? karena jika data di state berubah. React akan melakukan re-render (proses menampilkan komponen di browser)

Properties adalah sebuah data yang datang dari luar komponen.